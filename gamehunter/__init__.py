"""GameHunter — Telegram-бот для подбора видеоигр.

Бот помогает найти игру по интересам (жанрам), категориям, возрасту и франшизе
(IP), определяет регион пользователя по IP-адресу, спрашивает, во что пользователь
уже играл, и ведёт личную библиотеку: анкета, сыгранные игры, избранное и отзывы.

Проект построен на трёхслойной архитектуре:

    presentation   — слой представления (экраны бота, клавиатуры, тексты);
    domain         — слой бизнес-логики (сущности, сервисы, интерфейсы);
    infrastructure — слой доступа к данным (PostgreSQL через SQLAlchemy,
                     внешние API: RAWG, ipapi.co).

Зависимости направлены только внутрь: presentation -> domain <- infrastructure.
"""

__version__ = "2.0.0"
__project__ = "GameHunter"
